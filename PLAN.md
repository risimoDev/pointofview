# ViziAI — план развития и контекст сессии (обновлено 2026-07-12)

Файл для продолжения работы в новой сессии. Здесь: что сделано, что сломано,
что решено, и приоритетный план. Детали архитектуры — в CLAUDE.md и памяти
Claude (`~/.claude/projects/.../memory/`), диагностика — в `scripts/diag-*.sh`.

---

## 1. Текущее состояние (что работает)

**Прод**: домашний сервер Ubuntu (viziai-server, RTX 3070 8GB) + VPS (ruvds,
AmneziaWG-хаб 10.9.0.1, TLS nginx) + первая боевая точка ПВЗ (10.9.0.11,
4 камеры TP-Link, кассовый ПК Windows 7 как шлюз).

Сделано в этой сессии (всё в main, задеплоено частично — проверить на проде):
- **Live-видео**: go2rtc `<video-stream>` (завендорен, v1.9.14) MSE→HLS,
  один H264-источник на камеру, reconciler в API (60с) чинит go2rtc после
  рестартов. НЕ WebRTC (UDP через VPS — отказались), НЕ MJPEG (тяжёлый, ломался).
- **Статус камер честный**: heartbeat анализатора `camera_alive:{id}` (TTL 15с)
  → GET /cameras вычисляет online/offline. Дашборд поллит 15с.
- **UI полностью на русском**: энумы через `web/lib/labels.ts`.
- **Зоны**: полный CRUD + редактор (пороги dwell и т.д.).
- **События**: resolve, имена камер/зон, снапшоты, клипы.
- **Аналитика** (/analytics): график событий, разбивка по типам/камерам, посетители.
- **Алерты**: Telegram + webhook, тихие часы, мин. severity, тест-кнопка.
- **Re-ID («Сквозная идентификация», фича `reid`)** — НОВОЕ, не проверено на проде:
  - `analyzer/reid/`: embedder (HSV-гистограмма верх/низ тела; интерфейс под
    ONNX OSNet через env `REID_ONNX`) + IdentityManager.
  - Redis: `reid:gallery:{site_id}` (посетители, TTL 12ч), `reid:staff:{tenant}`
    (сотрудники, постоянно), `visitors:{tenant}` (посетители за день ПО ТОЧКЕ).
  - Сотрудники: не считаются посетителями, не триггерят queue_alert /
    zone_violation / crowd. Помечаются на странице /admin/people («Люди»)
    по фото-кропам (MinIO `reid/{tenant}/{gid}.jpg`, через POST /internal/reid/crop).
  - Посетители за день = уникальные личности на точку (конец двойного счёта камер).
  - Миграция `0003_reid_feature.sql`. Пороги в /admin/features.
- **Онбординг ПВЗ**: кит `pvz-onboarding/` — README (пошагово, двойной клик),
  CHECKLIST (там же реальные RTSP-адреса первой точки), `vps/add-site.sh`,
  Windows-скрипты ШАГ-1..5 (PowerShell 2.0-совместимые, UTF-8 BOM обязателен!).
- **Диагностика**: `scripts/diag-camera.sh` (одна камера по всей цепочке),
  `diag-tunnel-camera.sh` (камеры точки с сервера), `diag-stream*.sh`,
  `fix-vps-nginx-ws.sh` (WS-фикс на VPS — ПРИМЕНЯТЬ НА VPS, не на сервере).

## 2. Нерешённые проблемы (проверить в первую очередь)


1. **Re-ID не проверен на проде** — нужен деплой api+web+analyzer, включение
   фичи, прогон: пометить сотрудника, убедиться что посетители дедуплицируются.
   Пороги (0.88/0.90) наверняка потребуют подстройки по месту.
   ОБНОВЛЕНО 2026-07-13 (двойной счёт: «2 курьера ночью = 44 посетителя»):
   - причина: HSV-гистограммы слепнут на ночной ИК-картинке + личность
     создавалась с одного кадра при мерцании трекинга;
   - фиксы в analyzer/reid: пробация (min_samples=3, min_track_age=3с),
     фильтры качества (min_confidence, aspect ratio, min_crop 64px),
     гейт по насыщенности ночью (min_saturation=25, только для гистограмм),
     pending-треки не считаются посетителями (counter.py);
   - «Люди»: отметка сотрудника поглощает его дубли в галерее (cosine ≥
     match_threshold), добавлена кнопка «Удалить» личность;
   - нормальное решение ночи — OSNet ONNX: docs/REID-OSNET.md (экспорт,
     деплой, пороги 0.70/0.75 вместо 0.88/0.90), onnxruntime уже в
     requirements, REID_ONNX + /models уже в prod compose.
2. Деплой: `git pull && docker compose -f infra/docker-compose.prod.yml
   --env-file infra/.env.prod up -d --build api web analyzer` + миграции
   применяет `scripts/update.sh`.

## 3. Принятые решения (не пересматривать без причины)

- Транспорт видео: **MSE+HLS через /go2rtc/ прокси**, задержка 1-3с ок.
- Онбординг владельцев: **инвайт-ссылки** (не свободная регистрация).
- Профили организаций: **все 4** (ПВЗ полный, производство/ритейл/офис — частично dev).
- Шлюз точки: **Windows-ПК точки** (дёшево), но Win7 хрупок → для проблемных
  точек fallback = роутер/мини-ПК.
- Re-ID **без лиц** (по одежде/силуэту) — сознательно: без биометрии и 152-ФЗ
  проблем. Это козырь для гос/корп клиентов, НЕ менять на face recognition.
- Дизайн: без эмодзи, не-lucide иконки (@tabler), весь UI по-русски.
- `.ps1` с кириллицей — только UTF-8 с BOM (PS 5.1/2.0 иначе ломается).

## 4. План улучшений (приоритетный)

### Блок A — быстрые победы (по дню, делать первыми)
1. ~~**Алерт «камера не в сети»**~~ — СДЕЛАНО (2026-07-12, не проверено на проде):
   вотчдог в API (`api/src/camera_watchdog.ts`, тик 60с) следит за
   `camera_alive:{id}`; пропажа > `CAMERA_OFFLINE_ALERT_SECONDS` (300) →
   событие `camera_offline` (critical) в штатный конвейер (БД+WS+алерты),
   при восстановлении — `camera_online` (info). Камеры `file` и никогда не
   выходившие в онлайн не алармят. Миграция `0004_camera_offline_events.sql`.
   НА ПРОДЕ: после update.sh создать в /admin/alerts правило
   «Камера не в сети» с telegram chat_id; проверить отключением камеры.
2. **Ежедневная сводка в Telegram** (вечером): посетители, пик, события,
   необработанные. BullMQ cron. Владелец видит пользу каждый день.
3. **Метрики очереди/обслуживания в аналитике**: из `zone_exit.meta.dwell_sec`
   уже лежащих в БД — среднее ожидание в очереди, время у стойки, динамика.
   «Язык денег» для владельца.
4. **Детекция перекрытия камеры**: кадр чёрный/сдвинут (сравнение гистограммы
   с эталоном) → critical-алерт.

### Сделано 2026-07-13 (не проверено на проде)
- **Настройки сервера в админке** (/admin/settings, super): таблица
  `system_setting` (миграция 0005; БД имеет приоритет над .env) — порог
  «камера не в сети», ретеншен архива (дней + мин. свободно ГБ), ретеншен
  событий, клипы (pre/post roll, водяной знак), Telegram bot token (секрет,
  наружу не отдаётся). Панель состояния: занятость диска архива, размер БД,
  число событий/сегментов. Роуты GET/PUT /admin/settings, GET /admin/system.
- **Ретеншен** (`api/src/retention.ts`, раз в час): события — drop_chunks
  TimescaleDB; архив — удаление сегментов старше срока + аварийное удаление
  самых старых при нехватке места. API-контейнер теперь монтирует /archive rw.
- **Диск 1TB под архив**: пошаговая инструкция docs/ARCHIVE-DISK.md
  (монтировать прямо в ${DATA_ROOT}/archive → конфиги не меняются).

### Сделано 2026-07-13, вечер (анти-флуд + точность Re-ID; не проверено на проде)
Полное описание и роадмап дальше — **docs/AI-IMPROVEMENTS.md** (читать!).
- Анти-флуд: zone_entry/exit больше не алертят (consumer их не ставит в очередь);
  кулдаун алертов по global_id человека (не по камере); некритичные — сводкой
  раз в `alert_digest_minutes` (/admin/settings, деф. 30), critical сразу.
- Зоны: config.ignore_staff (деф. true), schedule.active_from/to +
  all_from/to («ночной режим» — алертить всех), состояние по личности,
  global_id в meta событий; редактор зон обновлён.
- Сотрудник: до 8 эталонов одежды ({embs}, legacy {emb} читается),
  «добавить к существующему сотруднику» на «Люди» (merge_into).
- Face-ID сотрудников: analyzer/reid/face.py (YuNet+SFace, CPU, модели
  качаются в Dockerfile с media.githubusercontent), очередь face_enroll,
  галерея face:staff:{tenant}; фото в карточке + автокроп при пометке;
  пороги face_threshold (0.36) / face_min_px (140) в конфиге фичи reid.
  Посетители по-прежнему БЕЗ биометрии (козырь сохранён).
- ДЕПЛОЙ: rebuild api+web+analyzer (analyzer качает face-модели — нужен
  интернет при сборке); МИГРАЦИЙ НЕТ; после старта нажать «Ресинхр Redis»
  в /admin/maintenance (иначе анализатор не получит tz и schedule зон);
  старые правила алертов на «Вход в зону» можно удалить — они мертвы.

### Сделано 2026-07-17 (фундамент многомодельности; не проверено на проде)
Разворот на производство: ADR-0001 (docs/architecture/), расчёт железа —
docs/CAPACITY-ANALYSIS.md (сервер 500к = б/у 3090 24ГБ, вариант B).
- **Абстракция детектора**: analyzer/detect/base.py (Detection, Detector,
  make_detector) + yolo.py; worker больше не знает про ultralytics.
- **Жизненный цикл плагинов**: setup/teardown в FeaturePlugin; PluginManager
  грузит модель в VRAM при включении фичи и выгружает при выключении —
  на 3070 8ГБ тестировать модели по одной/группами через /admin/features.
  Замер VRAM после setup; бюджет VRAM_BUDGET_MB (превышение → vram_exceeded,
  плагин не активируется). Статус → Redis plugin_status:{tenant} (TTL 120с).
- **model_version в meta каждого события** (детектор для zone-событий,
  модель плагина для плагинных). Метрики analyzer_metrics:{tenant}:
  infer_ms EMA, VRAM, число камер.
- **Плагин ppe** (каски/жилеты, зоны required_ppe): своя YOLO-модель из
  /models/ppe.pt (env PPE_MODEL или config.model), защиты: grace 10с,
  5 проверок подряд, min_confidence 0.6, min_person_px 120, кулдаун 300с
  по личности. НУЖНЫ ВЕСА: открытая hardhat-модель для отладки, боевая —
  файнтюн на данных пилота (обучение на 3070). Требуемый набор — в зоне
  (config.required), UI в редакторе зон (кнопки Каска/Жилет).
- **Плагин pose** (падение, yolov8n-pose из образа): угол корпуса от
  вертикали ≥65° или аспект bbox; 3 проверки подряд → fall_detected
  (critical). Миграция 0006 (feature_kind+=pose, event_type+=fall_detected).
- **/admin/features**: статус моделей (Работает/Ошибка/Не хватает VRAM, МБ),
  строка метрик анализатора; карточки ppe и pose с порогами.
- ДЕПЛОЙ: rebuild analyzer (качает yolov8n-pose при сборке) + api + web,
  миграция 0006 через update.sh; для СИЗ положить веса в
  ${DATA_ROOT}/models/ppe.pt. Смена пути модели в конфиге фичи применяется
  после выкл/вкл фичи.

### Сделано 2026-07-19 (отчёты ОТ + работа в одиночку; не проверено на проде)
Многомодельность проверена на проде: ppe (hardhat с HF, 45МБ) и pose (13МБ)
активны, VRAM-учёт и статусы работают. Модель каска+жилет — ждёт прогона
ноутбука на Kaggle (пользователь).
- **Отчёты по охране труда** (docs 14 §9 — «отчётность важнее детекции»):
  страница /reports (период, объект, карточки, таблицы, динамика);
  api/src/routes/reports.ts: GET /reports/safety(.pdf|.xlsx) + POST
  /reports/safety/telegram (sendDocument). PDF — pdfkit, шрифт DejaVu из
  api-образа (fonts-dejavu-core уже стоял), dev-Windows фолбэк Arial;
  Excel — exceljs («Сводка» + «Нарушения» до 5000 строк, для ГИТ).
  В отчёте: итоги, по типам/зонам/камерам, динамика, последние нарушения,
  model_version'ы за период. Типы: ppe_violation, fall_detected,
  zone_violation, crowd, lone_worker. Настройка «Telegram chat_id для
  отчётов» в /admin/settings. Общие RU-подписи API — event_labels.ts.
  НЕ в v1: по работникам (нужен СКУД), email (нет SMTP).
- **Работа в одиночку** (lone_worker, critical): CrowdPlugin, зона с
  config.min_people>=2 алармит при 0<людей<min_people дольше
  min_people_seconds (30с), кулдаун по зоне 300с; сотрудники СЧИТАЮТСЯ.
  Поле «Мин. людей в зоне» в редакторе зон. Миграция 0007. Работает
  только при включённой фиче crowd (это её плагин).
- ДЕПЛОЙ: git pull; rebuild api+web+analyzer; миграция 0007 через update.sh;
  задать chat_id отчётов в /admin/settings.

### Сделано 2026-07-19, вечер (сотрудники по лицу + аналитика; не проверено на проде)
- **Фикс отчётов**: enum=ANY(text[]) падал в PG → IN-литералы (reports.ts).
- **Face-first сотрудники** (analyzer/reid):
  - апскейл кропов <250px перед YuNet (мелкие лица на камерах ПВЗ);
  - пороги: face_min_px 140→100, face_interval 3с→2с;
  - АВТООБУЧЕНИЕ ОДЕЖДЫ: лицо подтвердило сотрудника → сегодняшний
    эмбеддинг одежды в reid:staff_auto:{tenant} (TTL 24ч, до 4/день) —
    дальше весь день узнаётся по одежде спиной/издалека;
  - поглощение фантомов: трек получил посетительскую личность, потом
    опознан как сотрудник → личность удаляется из галереи.
- **«Люди»**: форма «Добавить сотрудника» (имя → POST /people/staff, без
  ожидания камер) + фото лица по одному (очередь face_enroll; первое фото
  становится карточкой); статусы «лицо не обучено» / «N фото не
  распозналось». reid:staff_auto чистится при unstaff/delete.
- **Аналитика детально** (/analytics): среднее/макс ожидание в очереди и
  время у стойки (zone_exit.meta.dwell_sec), дельты к прошлому периоду,
  тепловая сетка «день недели × час», посетители по дням из новой таблицы
  visitor_daily (миграция 0008; снапшот Redis→PG каждые 10 мин,
  api/src/visitor_snapshot.ts; график наполняется с деплоя).
- ДЕПЛОЙ: rebuild api+web+analyzer, миграция 0008 (update.sh).
  ПРОВЕРКА НА ПВЗ: добавить сотрудника по имени + 2-3 фото анфас →
  подойти к камере лицом → в «Люди» посетитель-дубль исчезает, счётчик
  посетителей не растёт; после переодевания — узнаётся без лица.

### Сделано 2026-07-19, ночь (этап 0 производства: бэкапы + офлайн; не проверено)
- **Бэкапы** (закрыт критический долг I-01): scripts/backup.sh — ночной
  pg_dump -Fc + Redis SAVE в ${DATA_ROOT}/backups, ротация 7 дн + 4 вс,
  зеркало BACKUP_MIRROR_DIR (опция в .env.prod); scripts/restore.sh
  (с подтверждением, останавливает api/воркеры); установка cron —
  sudo scripts/install-backup-cron.sh (03:30). Статус в Redis backup:last →
  /admin/settings (панель состояния: дата/размер/просрочен/ошибка/не настроен).
- **Офлайн-поставка**: scripts/offline-bundle.sh (build + docker save всех
  образов + конфиги + миграции + install-offline.sh → один tar.gz);
  docs/OFFLINE-INSTALL.md. Analyzer печёт yolov8n/s.pt в /opt/models —
  в офлайне YOLO_MODEL=/opt/models/yolov8s.pt (путь, не имя!).
- Отложено решением владельца: зона по сигналу станка (АСУ ТП), email/SMTP.
- НА ПРОДЕ: git pull, rebuild analyzer (новые слои весов) + api + web;
  затем sudo ./scripts/install-backup-cron.sh и первый ./scripts/backup.sh
  руками; убедиться что в /admin/settings появился «Последний бэкап».

### Сделано 2026-07-20 (роли/права, Фаза 2 частично; не проверено на проде)
- **Модель прав — галочки возможностей** (shared PermissionCodes: live, events,
  analytics, reports, zones, cameras, people, alerts, features, users) +
  ограничение по камерам (allowed_camera_ids применяется в GET /cameras и
  событиях). permissions=null → легаси-дефолты роли (manager/operator).
  JWT несёт perms+cams → правки прав применяются при следующем входе.
  Гарды всех роутов пересажены на requirePerm; настройки сервера/диагностика/
  обслуживание — только super.
- **Роли**: super = платформа (кросс-тенантный, НЕ принадлежит организации);
  admin = «Владелец» (полный доступ к своему предприятию, создаёт пользователей,
  не может создать super). Миграция 0009 (name, permissions, disabled,
  user_invite).
- **Создание пользователей — оба способа**: инвайт-ссылка (владелец копирует,
  сотрудник сам ставит пароль, /invite/[token], accept в public API с
  rate-limit) и логин+пароль сразу. Отключение входа (disabled).
- **Платформа /admin/orgs (super)**: список организаций (+счётчики), создание
  (тенант+площадка+инвайт владельцу), «Войти» в организацию (JWT с imp,
  аудит; в шапке кнопка «← В платформу», куки super_token хранит обратный путь).
- **Web**: /admin/org — «Организация и доступы» (галочки, камеры, инвайты);
  навигация (верхняя и админская) гейтится по правам; /admin для не-super
  редиректит на /admin/org.
- ДЕПЛОЙ: rebuild api+web, миграция 0009. ВАЖНО: существующие
  manager/operator получат дефолтные наборы прав (см. RoleDefaultPerms).
- Дальше в этом пакете: сквозная UX-ревизия, детализация аналитики (остаток).

### Сделано 2026-07-20, VLM-описания событий (не проверено на проде)
- Модель: **qwen3-vl:4b** (Ollama, 3.3ГБ Q4 ≈ 4-4.5ГБ VRAM, лучший русский
  среди малых VLM; сменная через конфиг фичи vlm или env VLM_MODEL).
- Конвейер алертов стал consumer → **ai** → alerts (api/src/workers/ai.worker.ts):
  1) захват кадра из go2rtc → MinIO → event.snapshot_key — ЗАОДНО ПОЧИНЕНЫ
     СНАПШОТЫ СОБЫТИЙ (колонка была, но её никто не заполнял: фото в
     Telegram-алертах и кнопка «Кадр» на /events не работали);
  2) при включённой фиче vlm — описание кадра по-русски в
     meta.ai_description (+ai_model), таймаут 40с, best-effort: при падении
     Ollama алерт уходит без описания. Задержка алерта ≈ время генерации.
- Описание видно: в Telegram-алерте (строка после времени) и на /events
  под типом события. Карточка «ИИ-описания событий» в /admin/features
  (min_severity в конфиге, деф. warn).
- Compose: сервисы ollama (GPU, том ollama_data, модель выгружается после
  простоя) и worker-ai; OLLAMA_URL/VLM_MODEL в api_env. Миграция 0011.
- ДЕПЛОЙ: update.sh → up -d --build api worker-ai ollama (+пересборка
  worker-alerts/clips — общий образ). ПОТОМ ОДИН РАЗ:
  docker compose ... exec ollama ollama pull qwen3-vl:4b
  Включить фичу в /admin/features. Тест: вызвать critical-событие
  (падение/СИЗ) → в Telegram придёт алерт с фото и описанием; на /events
  у события появится кадр и текст. Без ollama pull фича молча даёт только
  снапшоты (лог worker-ai: "vlm describe failed").

### Сделано 2026-07-20, продолжение (саботаж камеры + тепловая карта)
- **Плагин tamper** (без моделей, CPU): перекрытие (яркость), ослепление,
  расфокус/плёнка (резкость < 25% собственной нормы камеры — адаптивно),
  сдвиг/поворот (корреляция с самообучающимся эталоном 64×36). 8 проверок
  подряд → camera_tampered (critical), кулдаун 600с по камере. Эталон
  адаптируется только на здоровой сцене. Известное ограничение: резкий
  переход день/ночь (ИК) может дать одно срабатывание. Логика проверена
  тестами (blackout/defocus/scene_change/recovery).
- **Плагин heatmap**: центры треков → сетка 48×27 → почасовые ключи
  heatmap:{camera}:{YYYYMMDDHH} (TTL 8 дней, flush раз в 60с);
  GET /cameras/:id/heatmap?hours → сумма; секция на /analytics с overlay
  поверх снапшота (canvas, sqrt-шкала teal→red). Конфиг: include_staff.
- Миграция 0010 (feature_kind += tamper, heatmap; event_type +=
  camera_tampered). Карточки обеих фич в /admin/features.
- НА ПРОДЕ: update.sh (0010) + rebuild analyzer/api/web; включить обе фичи;
  для саботажа — создать правило алерта; тест: прикрыть камеру рукой ~10с.

### Блок B — вау + продажи
5. **Тепловая карта**: накопление центров треков в Redis (сетка 32×18, по часам),
   overlay поверх кадра. Без новой модели. Закрывает dev-пункт heatmap.
6. **Расписание зон**: поле `zone.schedule` есть в БД, engine игнорирует.
   Включить → режим «ночная охрана» (человек ночью = critical).
7. **Лендинг** — СДЕЛАНО 2026-07-13 (не проверено на проде): публичная `/`
   (middleware пускает гостей; залогиненных шлёт в /dashboard).
   `web/components/landing/`: hero-сцена «взгляд камеры» v2 на anime.js 4.5 —
   процедурные скелеты позы (13 суставов, походка синхронизирована со
   скоростью), сценарии поведения (вход→очередь→стойка→выход, серый
   «сотрудник» за стойкой), глубина (масштаб/скорость по удалённости),
   target-lock рамки, шлейфы, параллакс за курсором (createAnimatable),
   hero уезжает при скролле; ВАЖНО: deltaTime таймера anime на первом тике
   бывает мусорным — зажат в [0,66] мс (иначе интеграция взрывается);
   prefers-reduced-motion = статичный кадр. Scroll-reveal, счётчики,
   мокап аналитики, 4 шага «как работает» (линия дорисовывается), форма
   «Запросить демо» → POST /api/v1/public/demo-request (rate-limit 5/час/IP)
   → Telegram в чат из настройки `lead_telegram_chat_id` (/admin/settings,
   без chat_id форма отвечает 503). ОСТАЛОСЬ из этого пункта: демо-режим
   (демо-аккаунт с file-камерами) + сценарий показа 5-7 мин.
   НА ПРОДЕ: задать «Telegram chat_id для заявок» в /admin/settings.
8. **OSNet ONNX для Re-ID** — почти готово: docs/REID-OSNET.md (экспорт на
   dev-ПК → scp в ${DATA_ROOT}/models → REID_ONNX в .env.prod → пороги
   0.70/0.75). Осталось выполнить руками и подстроить пороги.

### Блок C — локальный ИИ (VRAM: YOLO ~1ГБ, свободно ~6.5ГБ)
Контейнер Ollama/llama.cpp + очередь BullMQ `ai` (фоново, не риалтайм):
9. **VLM-описания событий** (Qwen2.5-VL-3B Q4, ~3ГБ): алерт с человеческим
   описанием кадра («у стойки 4 человека, ожидание ~3 мин»).
10. **VLM-верификация** перед алертом (отсев ложных срабатываний).
11. **Живая сводка текстом** (LLM поверх цифр дня).
12. **ИИ-чат по аналитике** (вопрос → SQL → ответ).
13. (дальше) Поиск по архиву на естественном языке (CLIP + pgvector).

### Блок D — крупное
14. **Видеоархив в браузере**: recorder уже пишет сегменты + archive_segment
    в БД; нужен UI (шкала времени, прыжок к событию). Самая ожидаемая фича.
15. **Фаза 2 — платформа**: таблица инвайтов, платформенный админ
    (кросс-тенантный), создание организаций с профилем, /invite/[token],
    операторы с allowed_camera_ids (поле есть, не применяется).
16. **Отчёты PDF/Excel на почту** (dev-пункт reports).
17. **CI/CD** (GitHub Actions → образы), бэкапы pg_dump + MinIO retention,
    мониторинг туннелей точек (пинг 10.9.0.x + алерт).

## 5. Полезное для новой сессии

- Рабочий процесс: код на dev-ПК (Windows) → git push → на сервере
  `git pull` + rebuild. У Claude НЕТ доступа к серверу/VPS/точке — диагностика
  через `.sh`-скрипты, вывод присылает пользователь. Отвечать по-русски.
- Реальные данные первой точки (камеры, RTSP, порты): `pvz-onboarding/CHECKLIST.md`.
- graphify: `graphify query "..."` для вопросов по коду; после правок `graphify update .`.
- Проверки перед коммитом: `npx tsc --noEmit` в api/ и web/,
  `python -m py_compile` для analyzer, парсер PS для .ps1 (см. память windows-ps-encoding).
- Память Claude: streaming-architecture, platform-model, pvz-onboarding,
  workflow-prod-testing, ui-localization, windows-ps-encoding, frontend-design,
  project-direction.
