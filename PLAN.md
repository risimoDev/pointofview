# ViziAI — план развития и контекст сессии (обновлено 2026-07-12)

Файл для продолжения работы в новой сессии. Здесь: что сделано, что сломано,
что решено, и приоритетный план. Детали архитектуры — в CLAUDE.md и памяти
Claude (`~/.claude/projects/.../memory/`), диагностика — в `scripts/diag-*.sh`.

---

## 1. Текущее состояние (что работает)

**Прод**: домашний сервер Ubuntu (viziai-server, RTX 3070 8GB) + VPS (ruvds,
AmneziaWG-хаб 10.9.0.1, TLS nginx) + первая боевая точка ПВЗ (10.9.0.11,
4 камеры TP-Link, кассовый ПК Windows 7 как шлюз).

Сделано в этой сессии (всё в main, задеплоено частично — проверить на проде):
- **Live-видео**: go2rtc `<video-stream>` (завендорен, v1.9.14) MSE→HLS,
  один H264-источник на камеру, reconciler в API (60с) чинит go2rtc после
  рестартов. НЕ WebRTC (UDP через VPS — отказались), НЕ MJPEG (тяжёлый, ломался).
- **Статус камер честный**: heartbeat анализатора `camera_alive:{id}` (TTL 15с)
  → GET /cameras вычисляет online/offline. Дашборд поллит 15с.
- **UI полностью на русском**: энумы через `web/lib/labels.ts`.
- **Зоны**: полный CRUD + редактор (пороги dwell и т.д.).
- **События**: resolve, имена камер/зон, снапшоты, клипы.
- **Аналитика** (/analytics): график событий, разбивка по типам/камерам, посетители.
- **Алерты**: Telegram + webhook, тихие часы, мин. severity, тест-кнопка.
- **Re-ID («Сквозная идентификация», фича `reid`)** — НОВОЕ, не проверено на проде:
  - `analyzer/reid/`: embedder (HSV-гистограмма верх/низ тела; интерфейс под
    ONNX OSNet через env `REID_ONNX`) + IdentityManager.
  - Redis: `reid:gallery:{site_id}` (посетители, TTL 12ч), `reid:staff:{tenant}`
    (сотрудники, постоянно), `visitors:{tenant}` (посетители за день ПО ТОЧКЕ).
  - Сотрудники: не считаются посетителями, не триггерят queue_alert /
    zone_violation / crowd. Помечаются на странице /admin/people («Люди»)
    по фото-кропам (MinIO `reid/{tenant}/{gid}.jpg`, через POST /internal/reid/crop).
  - Посетители за день = уникальные личности на точку (конец двойного счёта камер).
  - Миграция `0003_reid_feature.sql`. Пороги в /admin/features.
- **Онбординг ПВЗ**: кит `pvz-onboarding/` — README (пошагово, двойной клик),
  CHECKLIST (там же реальные RTSP-адреса первой точки), `vps/add-site.sh`,
  Windows-скрипты ШАГ-1..5 (PowerShell 2.0-совместимые, UTF-8 BOM обязателен!).
- **Диагностика**: `scripts/diag-camera.sh` (одна камера по всей цепочке),
  `diag-tunnel-camera.sh` (камеры точки с сервера), `diag-stream*.sh`,
  `fix-vps-nginx-ws.sh` (WS-фикс на VPS — ПРИМЕНЯТЬ НА VPS, не на сервере).

## 2. Нерешённые проблемы (проверить в первую очередь)

1. **Туннель первой точки СЛОМАН**: на Win7-ПК точки умер драйвер Wintun
   («Failed to install adapter device 0x1F»). Уже пробовали: перезагрузка,
   переустановка AmneziaWG, чистка драйвера (pnputil -f -d oem12.inf).
   Осталось попробовать: `netsh winsock reset` + `netsh int ip reset` + перезагрузка.
   Если не поможет — решение: перенести туннель на роутер (OpenWrt+amneziawg)
   или мини-ПК на Linux (~3000₽). Конфиг точки (`sites/mainpvz.conf` на VPS)
   переносится как есть.
2. **Камера «Склад»** (192.168.1.2, порт 554): локально на точке — «ОТЛИЧНО»,
   через туннель у ffprobe была «Invalid data...». Проброс верный
   (554→192.168.1.2). Не дочинили (упал туннель). Гипотеза MTU=1280 в
   [Interface] конфига точки НЕ проверена (попытка перезапуска туннеля и убила Wintun).
   ВАЖНО: не перезапускать службу туннеля без нужды — Wintun на Win7 хрупкий.
3. **Re-ID не проверен на проде** — нужен деплой api+web+analyzer, включение
   фичи, прогон: пометить сотрудника, убедиться что посетители дедуплицируются.
   Пороги (0.88/0.90) наверняка потребуют подстройки по месту.
4. Деплой: `git pull && docker compose -f infra/docker-compose.prod.yml
   --env-file infra/.env.prod up -d --build api web analyzer` + миграции
   применяет `scripts/update.sh`.

## 3. Принятые решения (не пересматривать без причины)

- Транспорт видео: **MSE+HLS через /go2rtc/ прокси**, задержка 1-3с ок.
- Онбординг владельцев: **инвайт-ссылки** (не свободная регистрация).
- Профили организаций: **все 4** (ПВЗ полный, производство/ритейл/офис — частично dev).
- Шлюз точки: **Windows-ПК точки** (дёшево), но Win7 хрупок → для проблемных
  точек fallback = роутер/мини-ПК.
- Re-ID **без лиц** (по одежде/силуэту) — сознательно: без биометрии и 152-ФЗ
  проблем. Это козырь для гос/корп клиентов, НЕ менять на face recognition.
- Дизайн: без эмодзи, не-lucide иконки (@tabler), весь UI по-русски.
- `.ps1` с кириллицей — только UTF-8 с BOM (PS 5.1/2.0 иначе ломается).

## 4. План улучшений (приоритетный)

### Блок A — быстрые победы (по дню, делать первыми)
1. ~~**Алерт «камера не в сети»**~~ — СДЕЛАНО (2026-07-12, не проверено на проде):
   вотчдог в API (`api/src/camera_watchdog.ts`, тик 60с) следит за
   `camera_alive:{id}`; пропажа > `CAMERA_OFFLINE_ALERT_SECONDS` (300) →
   событие `camera_offline` (critical) в штатный конвейер (БД+WS+алерты),
   при восстановлении — `camera_online` (info). Камеры `file` и никогда не
   выходившие в онлайн не алармят. Миграция `0004_camera_offline_events.sql`.
   НА ПРОДЕ: после update.sh создать в /admin/alerts правило
   «Камера не в сети» с telegram chat_id; проверить отключением камеры.
2. **Ежедневная сводка в Telegram** (вечером): посетители, пик, события,
   необработанные. BullMQ cron. Владелец видит пользу каждый день.
3. **Метрики очереди/обслуживания в аналитике**: из `zone_exit.meta.dwell_sec`
   уже лежащих в БД — среднее ожидание в очереди, время у стойки, динамика.
   «Язык денег» для владельца.
4. **Детекция перекрытия камеры**: кадр чёрный/сдвинут (сравнение гистограммы
   с эталоном) → critical-алерт.

### Блок B — вау + продажи
5. **Тепловая карта**: накопление центров треков в Redis (сетка 32×18, по часам),
   overlay поверх кадра. Без новой модели. Закрывает dev-пункт heatmap.
6. **Расписание зон**: поле `zone.schedule` есть в БД, engine игнорирует.
   Включить → режим «ночная охрана» (человек ночью = critical).
7. **Лендинг** (сайт начинается с логина — продукта «не существует» для гостя)
   + **демо-режим** (демо-аккаунт: file-камеры + симулятор + зоны преднастроены)
   + сценарий показа 5-7 мин. Для показа высокопоставленным лицам.
8. **OSNet ONNX для Re-ID** (env REID_ONNX уже поддержан) — точность.

### Блок C — локальный ИИ (VRAM: YOLO ~1ГБ, свободно ~6.5ГБ)
Контейнер Ollama/llama.cpp + очередь BullMQ `ai` (фоново, не риалтайм):
9. **VLM-описания событий** (Qwen2.5-VL-3B Q4, ~3ГБ): алерт с человеческим
   описанием кадра («у стойки 4 человека, ожидание ~3 мин»).
10. **VLM-верификация** перед алертом (отсев ложных срабатываний).
11. **Живая сводка текстом** (LLM поверх цифр дня).
12. **ИИ-чат по аналитике** (вопрос → SQL → ответ).
13. (дальше) Поиск по архиву на естественном языке (CLIP + pgvector).

### Блок D — крупное
14. **Видеоархив в браузере**: recorder уже пишет сегменты + archive_segment
    в БД; нужен UI (шкала времени, прыжок к событию). Самая ожидаемая фича.
15. **Фаза 2 — платформа**: таблица инвайтов, платформенный админ
    (кросс-тенантный), создание организаций с профилем, /invite/[token],
    операторы с allowed_camera_ids (поле есть, не применяется).
16. **Отчёты PDF/Excel на почту** (dev-пункт reports).
17. **CI/CD** (GitHub Actions → образы), бэкапы pg_dump + MinIO retention,
    мониторинг туннелей точек (пинг 10.9.0.x + алерт).

## 5. Полезное для новой сессии

- Рабочий процесс: код на dev-ПК (Windows) → git push → на сервере
  `git pull` + rebuild. У Claude НЕТ доступа к серверу/VPS/точке — диагностика
  через `.sh`-скрипты, вывод присылает пользователь. Отвечать по-русски.
- Реальные данные первой точки (камеры, RTSP, порты): `pvz-onboarding/CHECKLIST.md`.
- graphify: `graphify query "..."` для вопросов по коду; после правок `graphify update .`.
- Проверки перед коммитом: `npx tsc --noEmit` в api/ и web/,
  `python -m py_compile` для analyzer, парсер PS для .ps1 (см. память windows-ps-encoding).
- Память Claude: streaming-architecture, platform-model, pvz-onboarding,
  workflow-prod-testing, ui-localization, windows-ps-encoding, frontend-design,
  project-direction.
