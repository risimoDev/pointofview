# torch installed separately in Dockerfile (CUDA vs CPU wheel index)

# Main detector (Apache-2.0), Nano/Small/Medium/Large only — XL/2XL ship under
# a separate licence. See docs/commercial/01_LICENSE_REMEDIATION.md.
#
# DELIBERATELY NOT INSTALLED YET. `import rfdetr` in analyzer/detect/rfdetr.py
# is lazy, so the adapter sits inert while DETECTOR_KIND=yolo, and enabling it
# early would only add risk: rfdetr declares its own torch requirement, and
# `pip install` resolves it against the DEFAULT index — which can replace the
# CUDA wheel installed above with a CPU build and silently kill GPU inference.
# Enable together with the detector switch, and verify torch afterwards:
#   docker compose ... exec analyzer python -c \
#     "import torch; print(torch.__version__, torch.cuda.is_available())"
# rfdetr==1.3.*

# LEGACY, AGPL-3.0 — being removed. Still imported by plugins/pose.py and
# plugins/ppe.py; drop this line once both are migrated off ultralytics, and
# the on-premise licence blocker is closed.
# 8.4: required to load the YOLO26 PPE checkpoint (trained on 8.4.x).
ultralytics==8.4.*
supervision==0.25.*
opencv-python-headless==4.11.*
numpy==2.1.*
redis==5.2.*
httpx==0.28.*
pydantic==2.10.*
pydantic-settings==2.7.*
onnxruntime==1.20.*
